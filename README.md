# jorgevelez 

My own personle site. Custom site styled after Jeff Erickson's HTML layout. 

## Local development

```sh
rbenv local 3.0.0
bundle install
bundle exec jekyll serve
```

<!-- Edit locally 

rbenv exec bundle exec jekyll serve --livereload

 -->

Then open `http://localhost:4000`.
